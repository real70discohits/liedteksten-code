[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

markers =
    unit: fast, isolated tests of pure/component functions (no CLI, no subprocess)
    integration: end-to-end golden tests that run a real script against testdata
